//like the endgame climb selection not finished 
import { Text, View, StyleSheet } from "react-native";

interface SelectorButtonProps {
    label?: string;
    id?: number;
  }
  
function SelectorButton({ label, id}: SelectorButtonProps) {

}